//   34  is old enough to drive


/*
Write a function called "isPersonOldEnoughToDrive".
Given a "person" object, that contains an "age" property, "isPersonOldEnoughToDrive" returns whether the given person is old enough to drive.
Notes:
* The legal driving age in the United States is 16.
var output = isPersonOldEnoughToDrive(obj);
console.log(output); // --> true
*/

var obj1 = {
  age: 16
};
var obj2 = {
  age: 14
};

function isPersonOldEnoughToDrive(person) {
  // code here
}

console.log(isPersonOldEnoughToDrive(obj1)) // true
console.log(isPersonOldEnoughToDrive(obj2)) // false
